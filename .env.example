# HUMMBL-IO Environment Variables
# Copy this file to .env.local and fill in your actual values
# DO NOT commit .env.local to version control

# ============================================
# Frontend Environment Variables (Vite)
# ============================================
# Note: All Vite environment variables must be prefixed with VITE_
# to be accessible in the browser. See: https://vitejs.dev/guide/env-and-mode.html

# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
# Required for chat widget functionality
# Used in: src/App.tsx
VITE_OPENAI_API_KEY=your_openai_api_key_here

# Admin Authentication
# Password for admin access to the application
# Generate a secure password: openssl rand -base64 32
# Used in: src/utils/auth.ts
VITE_ADMIN_PASSWORD=your_secure_admin_password_here

# Data Loading Configuration
# Set to "false" to disable static data loading (defaults to true)
# Used in: src/utils/dataLoader.ts
VITE_USE_STATIC_DATA=true

# Build Output Directory
# Custom directory for build output (defaults to '/data')
# Used in: src/utils/dataLoader.ts
VITE_BUILD_OUTPUT_DIR=/data

# API Configuration
# Base URL for API requests (defaults to '/api')
# Used in: src/api/client.ts
VITE_API_BASE_URL=/api

# Caching Configuration
# Enable API response caching (defaults to false)
# Used in: src/api/client.ts
VITE_ENABLE_CACHING=false

# Cache TTL in milliseconds (defaults to 300000 = 5 minutes)
# Used in: src/api/client.ts
VITE_CACHE_TTL=300000

# Plausible Analytics Configuration (optional)
# Privacy-first analytics alternative to Google Analytics
# Get domain from: https://plausible.io/docs/plausible-script
# Used in: Analytics tracking (window.plausible)
VITE_PLAUSIBLE_DOMAIN=hummbl.io
VITE_PLAUSIBLE_API_KEY=your_plausible_api_key_here

# Sentry Error Tracking (Production only)
# Sentry is only enabled in production builds when all three variables are set
# Used in: vite.config.ts for source map uploads
# Get from: https://sentry.io/settings/account/api/auth-tokens/
VITE_SENTRY_ORG=your_sentry_org_slug
VITE_SENTRY_PROJECT=your_sentry_project_name
VITE_SENTRY_AUTH_TOKEN=your_sentry_auth_token

# ============================================
# Server Environment Variables (Node.js)
# ============================================
# These are only needed if running the server package
# Most features work with frontend-only setup

# Server Configuration
# Port for the backend server (defaults to 4000)
# Used in: server/index.ts, server/simple-server.js
PORT=4000

# Node Environment
# Options: development, production, test
# Used in: Multiple files for environment checks
NODE_ENV=development

# OpenAI API Key (for server-side usage)
# Used in: server/routes/proxy.ts, models that use OpenAI (e.g., src/models/p1/FirstPrinciplesModel.ts)
OPENAI_API_KEY=your_openai_api_key_here

# Anthropic API Key (optional, for Claude models)
# Used in: server/routes/proxy.ts, models that support Anthropic
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Supabase Configuration (optional)
# Required only if using Supabase authentication
# Get from: https://app.supabase.com/project/_/settings/api
# Used in: server/routes/auth.ts
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# JWT Secret
# Generate a secure random string for JWT token signing
# Used in: server/routes/auth.ts, server/middleware/authGuard.ts
# Generate: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_here

# Internal API Key (optional)
# Secret key for internal API authentication
# Used in: server/middleware/authGuard.ts
INTERNAL_API_KEY=your_internal_api_key_here

# ============================================
# Quick Start Guide
# ============================================
# 
# For local development, you only need:
# 1. VITE_OPENAI_API_KEY (if using chat features)
# 2. VITE_ADMIN_PASSWORD (for admin access)
#
# Optional but recommended:
# - VITE_USE_STATIC_DATA=true (default, can be omitted)
#
# ============================================
# Production Deployment (Vercel)
# ============================================
# 
# Set all VITE_* variables in Vercel dashboard:
# - Project Settings → Environment Variables
# - Add variables for Production, Preview, and Development environments
#
# Sentry variables are only needed for production error tracking:
# - Set VITE_SENTRY_ORG, VITE_SENTRY_PROJECT, VITE_SENTRY_AUTH_TOKEN
# - Sentry plugin will only activate in production builds
#
# ============================================
# Security Notes
# ============================================
# 
# 1. Never commit .env.local or .env files to version control
# 2. Use strong, randomly generated passwords and secrets
# 3. Rotate credentials regularly
# 4. Use different values for development and production
# 5. Store production secrets in Vercel dashboard, not in code
# 6. Review .gitignore to ensure .env files are excluded
